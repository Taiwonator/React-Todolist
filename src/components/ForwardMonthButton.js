import React, {Component} from 'react';

const ForwardMonthButton = (props) => (
    <div className='forwardMonthButton' onClick={props.forwardMonth}>
        <i />
        <i />
    </div>
)

export default ForwardMonthButton;