import React, {Component} from 'react';

const ForwardDayButton = (props) => (
    <div className='forwardDayButton' onClick={props.forwardDay}>
        <i />
    </div>
)

export default ForwardDayButton;